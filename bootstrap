#!/bin/sh
set -e
autoreconf --force --install -I config || exit 1
rm -rf autom4te.cache

# pull in uthash if we do not have it already
if test ! -f include/contrib/uthash/; then
  if type "git" &> /dev/null; then
    git submodule init
    git submodule update
  fi
fi
